# Page surfaces

The application uses state-driven page surfaces in `src/main.jsx` so the selected location and fetched weather state remain available across navigation. The page surfaces are Overview, Risk analysis, Climate map, Compare, Action planner, and Future risk.
